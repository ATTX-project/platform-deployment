apply plugin: 'com.chrisgahlert.gradle-dcompose-plugin'

dcompose {
	registry ("$registryURL") {
		// Needs no user/pass
	}

	attxes5 {
		baseDir = file('.')
		dockerFilename = 'Dockerfile'
		repository = "${imageRepo}:${imageRepoPort}/attx-es5:${imageES5}"
		attachStdout = true
	}
}
