apply plugin: "com.chrisgahlert.gradle-dcompose-plugin"

dcompose {
	registry ("$registryURL") {
		// Needs no user/pass
	}
	uvattxshared{
		baseDir = file('.')
		dockerFilename = 'Dockerfile'
		repository = "${imageRepo}:${imageRepoPort}/uv-attx-shared:latest"
		attachStdout = true
	}
}
